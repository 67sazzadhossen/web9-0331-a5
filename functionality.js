const perSeatPrice = 550; // Set the per-seat price
const maxSeatsAllowed = 4; // Set the maximum number of seats allowed

function seatSelection(seatId) {
    const selectedSeat = document.getElementById(seatId);
    // selectedSeat.classList.add('bg-[#1DD100]');
    
    selectedSeat.classList.add('bg-[#1DD100]');
    selectedSeat.classList.remove('bg-[#F7F8F8]');
   
    
   
}